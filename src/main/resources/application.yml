debug: true
logging.file: ./logs/organize.log
server:
  port: 21001
  tomcat.uri-encoding: UTF-8

spring:
  datasource:
    url: jdbc:mysql://10.154.96.67:3306/demo?useUnicode=true&amp;characterEncoding=utf8
    username: root
    password: cm123!@#
    driverClassName: com.mysql.jdbc.Driver
  jpa:
    database: MYSQL
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect : org.hibernate.dialect.MySQL5Dialect